meta {
  name: Test Non existing Route
  type: http
  seq: 3
}

get {
  url: {{connectionPath}}/admin/users
  body: none
  auth: bearer
}

auth:bearer {
  token: {{userToken}}
}

vars:pre-request {
  connectionPath: http://localhost:8085
}

settings {
  encodeUrl: true
  timeout: 0
}
